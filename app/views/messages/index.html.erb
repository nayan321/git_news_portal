<% @messages.each do |message| %>

<div class="container">
	<div class="col-md-4">
		<div class="news">
			<h2><%= message.title %></h2>
			<%= link_to "View Message", message_path(message), class: "btn btn-primary"%>

		</div>
	</div>
</div>

<% end %>